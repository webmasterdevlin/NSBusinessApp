android {
            defaultConfig {
                applicationId = "org.nativescript.NavApp"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }